version: '3.8'

services:
  mysql:
    image: mysql:5.7
    container_name: mysql_container
    environment:
      MYSQL_ROOT_PASSWORD: masterkey
      MYSQL_DATABASE: atl
      MYSQL_USER: wenjie
      MYSQL_PASSWORD: masterkey
    ports:
      - "3306:3306"
    restart: always